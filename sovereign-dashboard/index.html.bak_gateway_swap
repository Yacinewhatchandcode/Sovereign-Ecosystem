<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOVEREIGN GATEWAY | aSiReM World Flow</title>
    <link rel="stylesheet" href="sovereign_core.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@400;700&display=swap"
        rel="stylesheet">
    <style>
        body,
        html {
            height: 100%;
            margin: 0;
            background: #000;
            overflow: hidden;
        }

        .gateway-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('gateway_bg.png') no-repeat center center;
            background-size: cover;
            filter: brightness(0.6);
            z-index: -1;
            transition: transform 10s ease-out;
        }

        .gateway-background.zoom {
            transform: scale(1.1);
        }

        .content-overlay {
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(circle at center, transparent 0%, rgba(0, 0, 0, 0.8) 100%);
        }

        .title-container {
            margin-bottom: 50px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeIn 2s ease forwards;
        }

        h1 {
            font-family: var(--font-display);
            font-size: 4rem;
            font-weight: 900;
            letter-spacing: 15px;
            color: var(--text-bright);
            text-shadow: 0 0 30px var(--accent-green);
            margin-bottom: 10px;
        }

        .subtitle {
            font-family: var(--font-display);
            font-size: 1.2rem;
            letter-spacing: 5px;
            color: var(--accent-cyan);
            text-transform: uppercase;
        }

        .options-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            max-width: 1000px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeIn 2s ease 1s forwards;
        }

        .gateway-card {
            background: rgba(10, 10, 20, 0.4);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(0, 255, 157, 0.2);
            padding: 40px 20px;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .gateway-card:hover {
            border-color: var(--accent-green);
            transform: translateY(-10px);
            background: rgba(0, 255, 157, 0.05);
            box-shadow: 0 0 50px rgba(0, 255, 157, 0.2);
        }

        .card-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            display: block;
        }

        .card-title {
            font-family: var(--font-display);
            font-size: 1rem;
            font-weight: 700;
            letter-spacing: 2px;
            margin-bottom: 15px;
            color: var(--text-bright);
        }

        .card-desc {
            font-size: 0.8rem;
            color: var(--text-muted);
            line-height: 1.6;
        }

        .enter-portal {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, transparent, var(--accent-green), transparent);
            transform: scaleX(0);
            transition: transform 0.5s;
        }

        .gateway-card:hover .enter-portal {
            transform: scaleX(1);
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .asirem-status {
            position: fixed;
            bottom: 40px;
            left: 40px;
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 10px 20px;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 30px;
        }

        .status-dot {
            width: 12px;
            height: 12px;
            background: var(--accent-green);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--accent-green);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                opacity: 1;
            }

            50% {
                transform: scale(1.3);
                opacity: 0.5;
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }
    </style>
</head>

<body>
    <div class="gateway-background" id="bg"></div>

    <div class="content-overlay">
        <div class="title-container">
            <h1>aSiReM</h1>
            <div class="subtitle">Sovereign Knowledge Synthesis</div>
        </div>

        <div class="options-grid">
            <div class="gateway-card" onclick="location.href='/dashboard'">
                <span class="card-icon">🛰️</span>
                <div class="card-title">COMMAND CENTER</div>
                <div class="card-desc">Sovereign orchestrator for multi-agent fleet. Direct synaptic control.</div>
                <div class="enter-portal"></div>
            </div>

            <div class="gateway-card" onclick="location.href='/discovery'">
                <span class="card-icon">🌌</span>
                <div class="card-title">DISCOVERY MATRIX</div>
                <div class="card-desc">Semantic inventory of the sovereign codebase. Map the unseen flow.</div>
                <div class="enter-portal"></div>
            </div>

            <div class="gateway-card" onclick="location.href='/observability'">
                <span class="card-icon">🔭</span>
                <div class="card-title">OBSERVABILITY</div>
                <div class="card-desc">Deep-trace neural paths. Opik-powered multi-agent monitoring.</div>
                <div class="enter-portal"></div>
            </div>
        </div>
    </div>

    <div class="asirem-status">
        <div class="status-dot"></div>
        <div style="color: #fff; font-size: 0.9rem; font-family: var(--font-display);">MESH STATUS: ONLINE</div>
    </div>

    <script>
        // Subtle background zoom
        document.addEventListener('DOMContentLoaded', () => {
            setTimeout(() => {
                document.getElementById('bg').classList.add('zoom');
            }, 100);
        });
    </script>
</body>

</html>